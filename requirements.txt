# Pinned to the known-good versions running in production (verified 2026-07-08).
# Bump deliberately rather than letting an unrelated rebuild drift major versions.
fastapi==0.139.0
uvicorn[standard]==0.50.2
paho-mqtt==2.1.0
requests==2.34.2
pyyaml==6.0.3
